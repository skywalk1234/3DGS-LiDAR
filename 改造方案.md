# ReconDrive LiDAR 渲染改造方案

## 核心思路

**GS Head 基本不变，仅替换光栅化器为 SplatAD 版本。**

GS Head 仍然预测标准高斯参数（means, quats, scales, opacities, SH/features），
相机和 LiDAR 渲染**共享同一组高斯**。LiDAR 的 intensity/ray_drop 通过在
`lidar_rasterization()` 渲染后的 feature map 上接轻量 MLP 解码得到。

---

## 架构对比

```
改造前:
  images → VGGT → GS Head → 高斯参数 → rasterization() → RGB + depth
                                          │
                                     LiDAR点云 → 投影到稀疏深度图 → depth loss(仅监督)

改造后:
  images → VGGT → GS Head → 高斯参数 ──→ rasterization() → camera RGB + depth
                                       │
                                       └─→ lidar_rasterization() → depth + features
                                                                   ↓
                                                           lidar_decoder(MLP)
                                                                   ↓
                                                            intensity, ray_drop
```

---

## 修改清单

### 1. GS Head — 微调（非扩展）

**文件**: `models/vggt/heads/gs_dpt_head.py`

当前 GS Head 输出 `output_dim = 83`：
- rotation (4) + scale (3) + opacity (1) + SH (3×(4+1)²=75)

改动：将 SH 替换或扩展为通用 features（例如 32 维），这些 features 同时用于：
- 相机 RGB 渲染（通过 SH 解码或直接作为 RGB 特征）
- LiDAR rendering（直接作为 `lidar_features` 传给光栅化器）

具体方案（二选一）：

**方案 A（推荐）**：保持 SH 不变（75 维），额外加少量 feature 通道（如 8-16 维），
   `lidar_rasterization` 使用 `sh_degree=None` 的模式渲染 RGB + 附加 features。
   相机渲染走 SH → RGB，LiDAR 渲染走 features → intensity/ray_drop。

**方案 B**：将 SH 替换为 D 维通用 features，相机 RGB 也从 features 解码。
   与 SplatAD 完全一致。

### 2. 新增 LiDAR 光栅化模块

**文件**: `models/recondrive_model.py`

新增 `render_lidar()` 方法：

```python
def render_lidar(self, gs_params, lidar_data):
    means = rearrange(gs_params["means"], "b t v h w c -> b (t v h w) c")
    scales = rearrange(gs_params["scales"], ...)
    quats = rearrange(gs_params["quats"], ...)
    opacities = rearrange(gs_params["opacities"], ...)
    lidar_features = gs_params["lidar_features"]  # [B, N, D]

    render, alpha, alpha_sum, info = lidar_rasterization(
        means=means,
        quats=quats,
        scales=scales,
        opacities=opacities,
        lidar_features=lidar_features,  # [(C,) N, D]
        velocities=vels,
        viewmats=viewmat,               # [C, 4, 4]
        raster_pts=raster_pts,           # [C, H, W, 4]
        tile_elevation_boundaries=...,
        min_azimuth=-180, max_azimuth=180,
        n_elevation_channels=32,
        azimuth_resolution=0.1,
        ...
    )
    # render: [C, H, W, D+1], 最后一维是 expected depth
    depth = render[..., -1:]
    features = render[..., :-1]
    return {"depth": depth, "features": features, "alpha": alpha}
```

### 3. 新增 LiDAR 解码器

**文件**: 新建 `models/lidar_decoder.py` 或内联在 `recondrive_model.py`

参考 SplatAD（`splatad.py:364`）：

```python
self.lidar_decoder = MLP(
    in_dim=feature_dim + ray_dir_dim,  # features + ray direction
    out_dim=2,  # intensity, ray_drop
    ...
)
```

输入：渲染后的 features + LiDAR 射线方向 → intensity (sigmoid) + ray_drop (logits)

### 4. 新增 LiDAR 数据管线

**文件**: `dataset/vggt4dgs_scene_dataset.py`

从 NuScenes LiDAR 点云构建 `raster_pts`（azimuth/elevation 网格）：

```
raster_pts: [C, H, W, 4] = [azimuth, elevation, depth_init, time_offset]
```

此外还需输出：
- range image 真值（depth + intensity）
- tile_elevation_boundaries
- azimuth_resolution

### 5. 损失函数

**文件**: `models/loss_util.py`

新增 LiDAR 损失（参考 SplatAD）：

```
L_lidar = λ_depth · L1(pred_depth, gt_depth)
        + λ_intensity · L1(pred_intensity, gt_intensity)
        + λ_raydrop · BCE(pred_ray_drop, gt_ray_drop)
        + λ_los · LOS(pred_alpha_sum, gt_occupancy)  # line-of-sight loss
```

### 6. Pipeline 整合

**文件**: `models/recondrive_model.py` (training_step)

```python
def training_step(self, batch):
    gs_params = self.model.forward(batch["images"])

    # 相机渲染 (不变)
    cam_out = self.model.forward_renderer(gs_params, batch["camera"])

    # LiDAR 渲染 (新增)
    lidar_out = self.model.render_lidar(gs_params, batch["lidar"])

    # 联合损失
    loss = cam_loss(cam_out, batch["gt_images"]) + lidar_loss(lidar_out, batch["gt_lidar"])
```

### 7. 依赖更新

**文件**: `requirements.txt`

```diff
- gsplat==1.5.3
+ gsplat @ git+https://github.com/carlinds/splatad.git
```

---

## 与 SplatAD 的关键差异

| 方面 | SplatAD（优化式） | 本方案（前馈式） |
|---|---|---|
| 高斯来源 | 可学习参数，迭代优化 | VGGT 逐像素预测 |
| lidar_features | 可学习的 per-Gaussian 向量 `features_rest` | GS Head 预测的 features |
| 动态物体 | actor 边界框变换 | SAM2 + forward_flow 时间插值 |
| LiDAR 解码器 | `lidar_decoder(MLP)` | 相同：`lidar_decoder(MLP)` |
| 光栅化器 | `lidar_rasterization()` | 相同 |

---

## 不需要修改的部分

- `models/vggt/models/vggt.py` — VGGT backbone 不变
- `models/gaussian_util.py` — 相机渲染的辅助函数不变
- `models/geometry_util.py` — 投影工具
- `models/sh_rotation.py` — SH 旋转（若保留 SH）
- `models/compare.py` — 对比模型
